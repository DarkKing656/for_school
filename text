
diff_output=$(diff <(cat -b "$filename") <("./s21_cat" -b "$filename"))

line_num=0
while IFS= read -r diff_line; do
  if [[ $diff_line == *"<"* ]]; then
    echo "Строка $((++line_num))"
  fi
done <<< "$diff_output"
