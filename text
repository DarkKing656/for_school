file1="путь_к_первому_файлу"
file2="путь_к_второму_файлу"

# Чтение файлов построчно и сравнение строк
while IFS= read -r line1 && IFS= read -r line2 <&3; do
  if [[ "$line1" != "$line2" ]]; then
    echo "Строка $((++line_num)): $line2"
  fi
done <"$file1" 3<"$file2"
